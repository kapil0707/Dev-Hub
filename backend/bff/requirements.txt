# =============================================================================
# BFF Requirements
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# --- Web Framework -----------------------------------------------------------
fastapi==0.115.5
uvicorn[standard]==0.32.0        # ASGI server; [standard] includes WebSocket support

# --- HTTP Client (for calling downstream microservices) ----------------------
httpx==0.28.1                    # Async HTTP client; replaces 'requests' for async code

# --- gRPC Client (for calling Snippet Engine in Phase 3) ---------------------
grpcio==1.68.1
grpcio-tools==1.68.1             # protoc compiler for Python
protobuf==5.29.2

# --- Environment & Config ----------------------------------------------------
python-dotenv==1.0.1             # Load .env files

# --- Shared package (installed separately via pip install -e) ----------------
# devhub-shared  ← installed via: pip install -e ../../shared
